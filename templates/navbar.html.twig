<nav>
  <div class="navbar-content">
    <div class="logo">
      <a href="{{ path('app_home') }}">
        <img class="logo" src="{{ asset('/img/logo.gif') }}" alt="Logo">
      </a>
    </div>
    <div class="hyperlinks">
      <a class="btn" href="{{ path('app_home') }}">Strona główna</a>
{% if app.user %}
      <a class="btn" href="{{ path('app_list') }}">Lista wydarzeń</a>
{% if app.user and 'ROLE_ADMIN' not in app.user.roles %}
  <a class="btn" href="{{ path('app_user_ticket') }}">Lista kupionych biletów</a>
{% endif %}{% if 'ROLE_ADMIN' in app.user.roles %}
      <a class="btn" href="{{ path('app_admin') }}">EasyAdmin</a>
{% endif %}
      <a class="btn" href="{{ path('app_logout') }}">Wyloguj się</a>
{% else %}
      <a class="btn" href="{{ path('app_login') }}">Zaloguj się</a>
      <a class="btn" href="{{ path('app_register') }}">Zarejestruj się</a>
{% endif %}
    </div>
  </div>
</nav>
